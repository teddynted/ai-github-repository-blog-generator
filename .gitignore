# Build artifacts
/dist/
bootstrap
*.zip
# Compiled CLI binary from `go build ./cmd/mcp` at repo root
/mcp

# Local environment / secrets — never commit
.env
.env.*
!.env.example

# Go
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/
